using UnityEngine;
using PixelCrushers.DialogueSystem;

public class DialogueCheck : MonoBehaviour
{
    /*
    public bool IsDialogueActive { get; private set; }
    //public DialogueManager _dialogueManager; 
    public GameObject _dialogueSystemController; 
    void Start()
    {
        
    }
    private void OnEnable()
    {
        // Subscribe to dialogue events
        DialogueManager.onConversationStart
        DialogueManager.onConversationStart += StartDialogue;
        DialogueManager.onConversationEnd += EndDialogue;
    }

    private void OnDisable()
    {
        // Unsubscribe from dialogue events
        DialogueManager.onConversationStart -= StartDialogue;
        DialogueManager.onConversationEnd -= EndDialogue;
    }

    public void StartDialogue()
    {
        IsDialogueActive = true;
        // Other dialogue start logic
        Debug.Log("Dialogue Started");
    }

    public void EndDialogue()
    {
        IsDialogueActive = false;
        // Other dialogue end logic
        Debug.Log("Dialogue Ended");
    } */
}